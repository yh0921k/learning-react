import React from 'react';
import CounterContainer from './redux-middleware/containers/CounterContainer';

const App = () => {
  return (
    <div>
      <CounterContainer />
    </div>
  );
};

export default App;
