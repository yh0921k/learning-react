import React from 'react';
import SampleContainer from './redux-middleware/containers/SampleContainer';

const App = () => {
  return (
    <div>
      <SampleContainer />
    </div>
  );
};

export default App;
